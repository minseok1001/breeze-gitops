# Kubernetes Gateway API + Argo CD 설정 예시
# - 이 파일을 config.env로 복사해서 값을 채우세요.
#
#   cp k8s-setup/scripts/config.env.example k8s-setup/scripts/config.env
#   vi k8s-setup/scripts/config.env
#

# =========================
# kubeconfig / context
# =========================
# 자동 연결(클라우드 CLI 기반). 이미 kubeconfig가 있으면 스킵됩니다.
AUTO_CONNECT_K8S="true"
# (선택) kubeconfig 경로. 비워두면 기본(~/.kube/config)을 사용합니다.
KUBECONFIG=""
# (선택) 사용할 컨텍스트. 비워두면 현재 컨텍스트를 사용합니다.
KUBE_CONTEXT=""
# (선택) Kubernetes 프로바이더. 비워두면 자동 감지 (aws, gcp, azure, generic).
KUBERNETES_PROVIDER=""

# =========================
# AWS / EKS
# =========================
# (선택) EKS 클러스터 이름. 지정하면 kubeconfig 자동 업데이트를 시도합니다.
EKS_CLUSTER_NAME=""
# (선택) AWS 리전. EKS 클러스터 업데이트 시 사용.
AWS_REGION="us-east-1"

# =========================
# GCP / GKE
# =========================
# (선택) GKE 클러스터 이름.
GKE_CLUSTER_NAME=""
# (선택) GCP 프로젝트 ID.
GKE_PROJECT=""
# (선택) GKE 위치(리전 또는 존).
GKE_LOCATION=""
# (선택) GKE 위치 타입: region 또는 zone. 비워두면 자동 추정.
GKE_LOCATION_TYPE=""

# =========================
# Azure / AKS
# =========================
# (선택) AKS 클러스터 이름.
AKS_CLUSTER_NAME=""
# (선택) AKS 리소스 그룹.
AKS_RESOURCE_GROUP=""
# (선택) Azure 구독 ID.
AZURE_SUBSCRIPTION_ID=""

# =========================
# Gateway API
# =========================
ENABLE_GATEWAY_API="true"
# Gateway API 버전(고정)
GATEWAY_API_VERSION="v1.1.0"
# 설치 매니페스트(우선순위: PATH > URL > 기본 URL)
GATEWAY_API_MANIFEST_PATH=""
GATEWAY_API_MANIFEST_URL=""

# =========================
# Argo CD
# =========================
ENABLE_ARGOCD="true"
# Argo CD 버전(고정)
ARGOCD_VERSION="v2.12.6"
ARGOCD_NAMESPACE="argocd"
# 설치 매니페스트(우선순위: PATH > URL > 기본 URL)
ARGOCD_MANIFEST_PATH=""
ARGOCD_MANIFEST_URL=""
# true면 주요 디플로이가 Ready 될 때까지 대기
ARGOCD_WAIT="true"
